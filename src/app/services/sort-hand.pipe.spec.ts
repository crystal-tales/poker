import { SortHandPipe } from './sort-hand.pipe';

describe('SortHandPipe', () => {
    it('create an instance', () => {
        const pipe = new SortHandPipe();
        expect(pipe).toBeTruthy();
    });
});
